---
phase: 09-node-layer
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: false
requirements: [NODE-01, NODE-02, NODE-03]
must_haves:
  truths:
    - "User sees enlarged, crisp node markers at default browser zoom"
    - "User sees staggered tick heights when objectives share the same date"
    - "User reads the latest node stage label without zooming"
    - "User sees terminal nodes with geometric SVG icons, not blurry emoji"
    - "User sees a subtle breathing pulse on the latest node per objective"
    - "User can read origin date labels in dark mode without straining"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 9 node layer improvements across light mode, dark mode, and the full Sandoz timeline.

Purpose: NODE-01/02/03 are perceptual requirements — legibility, collision avoidance, and icon quality cannot be fully verified by unit tests. Human review confirms the visual output matches the design contract.

Output: Approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-node-layer/09-CONTEXT.md
@.planning/phases/09-node-layer/09-UI-SPEC.md
@.planning/phases/09-node-layer/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual review of node layer improvements</name>
  <files>none</files>
  <action>
Start the dev server and present the Sandoz timeline for human visual review. Run `cd frontend && npm run dev` and open the company page.

**What was built (Plan 09-01):**
- Terminal nodes (PROVED / SILENT DROP) now use crisp SVG geometric icons instead of emoji
- Signal nodes are dot-only (no labels) — decluttered
- Latest node per objective has larger radius (r=8/4), fontSize=11 stage label, and breathing pulse animation on outer halo
- Proximity-bucket stagger: nodes at the same x-position get staggered tick heights (20/32/44/56px)
- Stale and fiscal-year-end nodes use CSS variable instead of hardcoded hex
- Dark mode muted-foreground upgraded to #9ca3af for WCAG AA compliance

**Verification checklist for the user:**

1. Start dev server: `cd frontend && npm run dev`
2. Open http://localhost:3000/company/sdz in your browser at 100% zoom

**Light mode checks:**
- [ ] Terminal nodes (rightmost on buried objectives) show geometric icons (upward chevron for PROVED, downward triangle for buried) — NOT emoji
- [ ] Signal dots along each objective path are small (r=5 halo) with NO labels — only dots and ticks
- [ ] The LATEST node per objective (rightmost signal) is visibly larger than other signals and shows a stage label like "FLY +3" at readable size
- [ ] The latest node outer ring has a subtle breathing pulse animation (slow scale oscillation)
- [ ] Where objectives share a date, tick heights are staggered (not all at the same height)
- [ ] Origin nodes still show date labels (e.g., "Oct 2023")

**Dark mode checks (toggle theme):**
- [ ] Origin date labels are clearly legible against the dark canvas (not near-invisible)
- [ ] All node colours render correctly in dark mode
- [ ] Pulse animation is visible on latest nodes

**Regression checks:**
- [ ] Ground line is still visible
- [ ] Spline paths and area fills are intact
- [ ] Time range pills (6M/1Y/2Y/All) still work
- [ ] Tooltips still appear on hover

Resume signal: Type "approved" if all checks pass, or describe any issues you see.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>User visually confirms NODE-01 (icon quality), NODE-02 (stagger/declutter), NODE-03 (legibility) are satisfied with no regressions.</done>
</task>

</tasks>

<verification>
Human visual approval of all NODE-01, NODE-02, NODE-03 requirements.
</verification>

<success_criteria>
- User confirms node markers are legible at default zoom (NODE-01)
- User confirms no label collisions with staggered ticks (NODE-02)
- User confirms stage labels readable on latest nodes (NODE-03)
- No visual regressions in paths, fills, ground line, or tooltips
</success_criteria>

<output>
After completion, create `.planning/phases/09-node-layer/09-02-SUMMARY.md`
</output>
