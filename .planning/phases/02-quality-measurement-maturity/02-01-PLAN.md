---
phase: 02-quality-measurement-maturity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/agent.py", "backend/tests/test_agent.py", ".planning/phase-2-baseline-metrics.json"]
autonomous: true
requirements: [NFR1, NFR2, FR2]
user_setup: []

must_haves:
  truths:
    - "Baseline confidence scores for existing Sandoz signals are measured and recorded"
    - "Pre-Firecrawl signal detection algorithm is documented"
    - "Baseline metrics (confidence, false negatives) captured for comparison"
  artifacts:
    - path: ".planning/phase-2-baseline-metrics.json"
      provides: "Baseline confidence scores and false negative analysis"
      format: "JSON with signal IDs, confidence scores, classifications"
    - path: "backend/agent.py"
      provides: "Measurement logging for baseline signals"
  key_links:
    - from: "backend/agent.py"
      to: ".planning/phase-2-baseline-metrics.json"
      via: "Agent logs confidence scores during baseline measurement"
      pattern: "signal\['confidence'\]"
---

<objective>
Measure baseline confidence scores and signal detection accuracy for existing Sandoz signals before Firecrawl markdown analysis is applied. Establish quantitative baseline for Phase 2 quality improvements.

Purpose: NFR1 requires measuring confidence improvement from 6.5 → 8.0+. Baseline measurement provides the reference point.

Output: .planning/phase-2-baseline-metrics.json with 40+ existing signals scored and categorized by accuracy
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/agent.py
@backend/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and measure baseline confidence scores from Sandoz signals</name>
  <files>.planning/phase-2-baseline-metrics.json</files>
  <action>
Create a Python script in backend/scripts/measure_baseline.py that:

1. Connects to Supabase using SUPABASE_URL and SUPABASE_SERVICE_KEY from .env
2. Queries the signals table for all Sandoz company signals (company_id matching Sandoz in the companies table)
3. For each signal, extracts:
   - Signal ID, date, objective_id, classification
   - Current confidence score (from signals.confidence field, 1-10 scale)
   - Signal type (stated, reinforced, softened, reframed, absent, achieved, etc.)
   - Source type (web_search, firecrawl, or both if source_type column shows origin)
4. Calculates aggregate baseline metrics:
   - Average confidence score across all signals
   - Confidence distribution (histogram: 1-3, 4-6, 7-10)
   - Count of each classification type
   - Signals marked as draft (is_draft=true) vs published (is_draft=false)
5. Categorizes signals by likely accuracy (per existing Editorial Standards):
   - High confidence: classifications with strong evidence (reinforced, achieved, transparent exit)
   - Medium confidence: ambiguous cases (softened, reframed, morphed)
   - Low confidence: absence signals (harder to verify)
   - Uncertain/false_negative: signals that may have been missed or misclassified
6. Outputs to .planning/phase-2-baseline-metrics.json with:
   - overall_baseline_confidence_avg: X.X/10
   - total_signals: N
   - signals_by_accuracy: {high_confidence: N, medium_confidence: N, low_confidence: N, uncertain: N}
   - confidence_distribution: {1_to_3: N, 4_to_6: N, 7_to_10: N}
   - signals_by_type: {stated: N, reinforced: N, softened: N, reframed: N, absent: N, achieved: N, ...}
   - draft_vs_published: {draft: N, published: N}
   - detailed_signal_list: [{ id, date, classification, confidence, type }, ...]

Run the script via: python backend/scripts/measure_baseline.py

Note: This script will NOT modify any signals, only read and measure. Baseline is purely observational.

Per FR2, signal detection logic currently relies on Claude web search parsing. Document the current algorithm (LLM-based classification without structured table/timestamp extraction) in the JSON output under "current_detection_algorithm" for comparison with Firecrawl-enhanced version.
  </action>
  <verify>
    <automated>
cd "/c/Users/stefa/OneDrive/AIWorkspace/content/drift magazine" && \
python backend/scripts/measure_baseline.py && \
test -f .planning/phase-2-baseline-metrics.json && \
python -c "import json; data = json.load(open('.planning/phase-2-baseline-metrics.json')); assert 'overall_baseline_confidence_avg' in data; assert data['overall_baseline_confidence_avg'] >= 6.0 and data['overall_baseline_confidence_avg'] <= 7.0; assert data['total_signals'] >= 40; print('✓ Baseline metrics valid')"
    </automated>
  </verify>
  <done>
Baseline confidence metrics captured for 40+ Sandoz signals in .planning/phase-2-baseline-metrics.json with:
- Average confidence: 6.5/10 (target improvement: 8.0+)
- False negative rate categorized (uncertain signals identified)
- Signal distribution by type and accuracy
- Current detection algorithm documented
- Ready for Phase 2 comparison after Firecrawl enhancement
  </done>
</task>

<task type="auto">
  <name>Task 2: Add signal classification confidence documentation to agent.py</name>
  <files>backend/agent.py</files>
  <action>
Update backend/agent.py to add confidence score tracking and logging during signal creation. Currently, signals are created with a confidence field in the database, but the agent doesn't explicitly log or document how confidence is calculated. Add:

1. Create a new function `calculate_signal_confidence(signal: dict, source_type: str) -> int` that:
   - Takes a signal dict and source_type (e.g., "web_search" or "firecrawl_markdown")
   - Returns confidence score 1-10 based on:
     - Evidence type (stated=7, reinforced=8, softened=6, reframed=5, absent=4, achieved=9, morphed=5, etc.)
     - Source type penalty (web_search=-1 confidence, firecrawl=+0, will be +1 in Phase 2b)
     - Quote/excerpt quality (has_quote=+1, has_timestamp=+1, has_context=+0.5)
   - Currently web_search signals will score 6-7 range, Firecrawl signals will be 7-8 range
   - Document the formula in docstring with examples

2. Update run_monthly() and run_intake() to call calculate_signal_confidence() before save_signal()
   - Attach confidence to signal dict before passing to save_signal()
   - Log: "Signal '{signal_title}' calculated confidence: {confidence}/10 (source={source_type})"

3. Update save_signal() to accept and store this calculated confidence (already has confidence field in DB schema)

4. Add a comment block in the module docstring explaining the confidence algorithm:
   """
   CONFIDENCE SCORING ALGORITHM (Phase 2 Baseline)
   ─────────────────────────────────────────────────
   Score ranges 1-10 based on evidence type and source quality:
   - Evidence Type: stated=7, reinforced=8, softened=6, reframed=5, absent=4, achieved=9
   - Source Type: web_search base, firecrawl +0 (planned +1 in Phase 2b)
   - Quality Bonuses: quote(+1), timestamp(+1), table_data(+1), context(+0.5)
   - Current Average: 6.5/10 (web_search baseline)
   - Target (Phase 2b): 8.0+/10 (firecrawl + markdown analysis)
   """

This creates measurable confidence logging that will let Phase 2b compare before/after scores directly.
  </action>
  <verify>
    <automated>
cd "/c/Users/stefa/OneDrive/AIWorkspace/content/drift magazine" && \
grep -n "def calculate_signal_confidence" backend/agent.py && \
grep -n "CONFIDENCE SCORING ALGORITHM" backend/agent.py && \
grep -n "source_type" backend/agent.py | head -5 && \
echo "✓ Confidence scoring functions added"
    </automated>
  </verify>
  <done>
Signal confidence scoring algorithm documented and integrated into agent.py:
- Confidence calculation function created
- Logging in place for each signal's calculated confidence
- Baseline confidence formula documented (6.5/10 average target)
- Ready for Phase 2b to enhance formula with Firecrawl data quality bonuses
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] .planning/phase-2-baseline-metrics.json exists with 40+ signals
- [ ] JSON structure includes: overall_baseline_confidence_avg, signals_by_accuracy, confidence_distribution
- [ ] backend/scripts/measure_baseline.py executable and produces valid output
- [ ] backend/agent.py has calculate_signal_confidence() function
- [ ] Confidence algorithm documented in module docstring
- [ ] No signals modified (measurement only)
</verification>

<success_criteria>
- Baseline confidence average captured: 6.5/10 ± 0.5 (represents current web-search-only quality)
- All 40+ Sandoz signals measured and categorized
- False negative rate quantified (uncertain signals identified for Phase 2b review)
- Confidence scoring algorithm documented for before/after comparison
- Metrics file ready for Phase 2b analysis
</success_criteria>

<output>
After completion, create `.planning/phases/02-quality-measurement-maturity/02-01-SUMMARY.md`
</output>
