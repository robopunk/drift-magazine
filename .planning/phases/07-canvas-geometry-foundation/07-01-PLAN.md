---
phase: 07-canvas-geometry-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/company/TimelineCanvas.tsx
autonomous: false
requirements:
  - CANVAS-03

must_haves:
  truths:
    - "User scrolls to leftmost/rightmost timeline points and sees no node, label, or tick cut off"
    - "User sees top score labels (Orbit +4, Fly +3) fully visible with clearance above them"
    - "User sees ground line and below-ground zone at the correct vertical position after canvas height increase"
    - "STAGE_HEIGHT remains exactly 68.75px — stage spacing unchanged"
  artifacts:
    - path: "frontend/src/components/company/TimelineCanvas.tsx"
      provides: "Updated canvas geometry constants"
      contains: "PADDING_Y = 60"
  key_links:
    - from: "PADDING_Y constant"
      to: "STAGE_HEIGHT, GROUND_Y, scoreToY, zone rects, axis labels"
      via: "formula chain: STAGE_HEIGHT = (CANVAS_HEIGHT - PADDING_Y - AXIS_LABEL_HEIGHT) / 8"
      pattern: "PADDING_Y = 60"
    - from: "CANVAS_HEIGHT constant"
      to: "outer container height, label column SVG, main SVG"
      via: "CANVAS_HEIGHT + 44 container, height={CANVAS_HEIGHT} SVGs"
      pattern: "CANVAS_HEIGHT = 650"
    - from: "canvasWidth calculation"
      to: "SVG width, zone rects, grid lines"
      via: "HORIZONTAL_PADDING added to totalMonths * MONTH_WIDTH"
      pattern: "HORIZONTAL_PADDING"
---

<objective>
Update canvas geometry constants in TimelineCanvas.tsx so every element (nodes, labels, ticks, axis text) is fully visible with no edge clipping at any canvas boundary.

Purpose: Phase 7 is the foundation for all subsequent v4.2 phases. Correct padding and dimensions must land first because Phases 8, 9, and 10 all depend on stable geometry constants.
Output: Updated TimelineCanvas.tsx with new PADDING_Y, CANVAS_HEIGHT, and horizontal padding — all derived values cascading correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-canvas-geometry-foundation/07-CONTEXT.md

@frontend/src/components/company/TimelineCanvas.tsx

<interfaces>
<!-- Current constants (lines 46-52 of TimelineCanvas.tsx) — these are what we modify -->
const PADDING_Y = 30;
const CANVAS_HEIGHT = 620;
const AXIS_LABEL_HEIGHT = 40;
const STAGE_HEIGHT = (CANVAS_HEIGHT - PADDING_Y - AXIS_LABEL_HEIGHT) / 8;  // = 68.75
const GROUND_Y = PADDING_Y + 4 * STAGE_HEIGHT;  // = 305
const MONTH_WIDTH = 40;
const LABEL_COL_WIDTH = 60;

<!-- Key downstream references that auto-cascade -->
function scoreToY(score: number): number {
  return PADDING_Y + (4 - score) * STAGE_HEIGHT;
}

<!-- Container height (line 345) -->
style={{ height: CANVAS_HEIGHT + 44, minHeight: 500 }}

<!-- Canvas width calculation (line 123) -->
const canvasWidth = Math.max(totalMonths * MONTH_WIDTH, 800);

<!-- Node x positioning (line 147) -->
node.x = (originOffset + i) * MONTH_WIDTH + MONTH_WIDTH / 2;

<!-- Top axis labels (lines 484, 497) -->
y={PADDING_Y - 14}  <!-- score label -->
y={PADDING_Y - 4}   <!-- score sublabel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update canvas geometry constants and add horizontal padding</name>
  <files>frontend/src/components/company/TimelineCanvas.tsx</files>
  <read_first>
    - frontend/src/components/company/TimelineCanvas.tsx (full file — must see all constant usages before modifying)
    - .planning/phases/07-canvas-geometry-foundation/07-CONTEXT.md (decisions D-01 through D-05)
  </read_first>
  <action>
Modify the constants block at lines 46-52 of TimelineCanvas.tsx. Make these exact changes:

**1. PADDING_Y (per D-01):** Change from 30 to 60.
```typescript
const PADDING_Y = 60;
```

**2. CANVAS_HEIGHT (per D-02):** Change from 620 to 650.
```typescript
const CANVAS_HEIGHT = 650;
```

**3. Add HORIZONTAL_PADDING constant (per D-04):** Add a new constant after LABEL_COL_WIDTH:
```typescript
const HORIZONTAL_PADDING = 40;
```

**4. Update canvasWidth calculation (per D-04):** At line 123, change:
```typescript
// FROM:
const canvasWidth = Math.max(totalMonths * MONTH_WIDTH, 800);
// TO:
const canvasWidth = Math.max(totalMonths * MONTH_WIDTH + HORIZONTAL_PADDING * 2, 800);
```

**5. Offset node x-positions by HORIZONTAL_PADDING (per D-04):** At line 147, change:
```typescript
// FROM:
node.x = (originOffset + i) * MONTH_WIDTH + MONTH_WIDTH / 2;
// TO:
node.x = (originOffset + i) * MONTH_WIDTH + MONTH_WIDTH / 2 + HORIZONTAL_PADDING;
```

Also update all other x-position calculations that use the same MONTH_WIDTH pattern to include the HORIZONTAL_PADDING offset. Specifically check:
- Terminal node x calculation (~line 169): add `+ HORIZONTAL_PADDING`
- Signal dot x calculations (~line 213, ~line 230, ~line 245): add `+ HORIZONTAL_PADDING`
- Month tick x positions in the axis rendering — any `i * MONTH_WIDTH + MONTH_WIDTH / 2` pattern: add `+ HORIZONTAL_PADDING`
- Today-line x position: add `+ HORIZONTAL_PADDING` if it uses the same month-to-x conversion
- FY marker x positions: add `+ HORIZONTAL_PADDING` if they use month-to-x conversion

**DO NOT change:**
- AXIS_LABEL_HEIGHT (stays at 40)
- MONTH_WIDTH (stays at 40)
- LABEL_COL_WIDTH (stays at 60)
- The STAGE_HEIGHT formula (it auto-computes to 68.75 from the new values: (650 - 60 - 40) / 8 = 68.75)
- The GROUND_Y formula (it auto-computes to 335 from: 60 + 4 * 68.75 = 335)
- The scoreToY function (it uses PADDING_Y and STAGE_HEIGHT which auto-cascade)
- The container height formula at line 345 (CANVAS_HEIGHT + 44 auto-cascades to 694)
- The label column SVG height at line 364 (height={CANVAS_HEIGHT} auto-cascades to 650)

**Cascade verification (per D-05):** After the change, these derived values must hold:
- STAGE_HEIGHT = (650 - 60 - 40) / 8 = 68.75 (unchanged)
- GROUND_Y = 60 + 4 * 68.75 = 335 (was 305)
- Container height = 650 + 44 = 694 (was 664)
- Top axis label y = 60 - 14 = 46 (was 16, now safely inside SVG)
- Top axis sublabel y = 60 - 4 = 56 (was 26, now safely inside SVG)
  </action>
  <verify>
    <automated>cd "C:\Users\stefa\OneDrive\AIWorkspace\content\drift magazine" && grep -n "PADDING_Y = 60" frontend/src/components/company/TimelineCanvas.tsx && grep -n "CANVAS_HEIGHT = 650" frontend/src/components/company/TimelineCanvas.tsx && grep -n "HORIZONTAL_PADDING = 40" frontend/src/components/company/TimelineCanvas.tsx && grep -n "HORIZONTAL_PADDING \* 2" frontend/src/components/company/TimelineCanvas.tsx && echo "All constants verified"</automated>
  </verify>
  <acceptance_criteria>
    - TimelineCanvas.tsx contains `const PADDING_Y = 60;`
    - TimelineCanvas.tsx contains `const CANVAS_HEIGHT = 650;`
    - TimelineCanvas.tsx contains `const HORIZONTAL_PADDING = 40;`
    - canvasWidth line contains `HORIZONTAL_PADDING * 2`
    - Node x-position line contains `+ HORIZONTAL_PADDING`
    - STAGE_HEIGHT formula unchanged: `(CANVAS_HEIGHT - PADDING_Y - AXIS_LABEL_HEIGHT) / 8`
    - GROUND_Y formula unchanged: `PADDING_Y + 4 * STAGE_HEIGHT`
    - AXIS_LABEL_HEIGHT unchanged at 40
    - MONTH_WIDTH unchanged at 40
    - No other files modified
  </acceptance_criteria>
  <done>All three constants updated (PADDING_Y=60, CANVAS_HEIGHT=650, HORIZONTAL_PADDING=40), canvasWidth includes horizontal padding, all node/signal/tick x-positions offset by HORIZONTAL_PADDING, all derived values cascade correctly with STAGE_HEIGHT remaining at 68.75</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of canvas geometry</name>
  <files>frontend/src/components/company/TimelineCanvas.tsx</files>
  <read_first>
    - frontend/src/components/company/TimelineCanvas.tsx (verify the constants are updated before starting dev server)
  </read_first>
  <action>
Start the dev server and present the timeline canvas for visual verification. Run `cd frontend && npm run dev`. The user will verify that:
1. Top axis labels (Orbit +4, Fly +3) are fully visible with clearance above
2. Left and right edge nodes/labels are not clipped
3. Ground line is at the correct position (GROUND_Y = 335)
4. Stage spacing is even (8 rows, 68.75px each)
5. Below-ground zone extends correctly
  </action>
  <verify>
    <automated>cd "C:\Users\stefa\OneDrive\AIWorkspace\content\drift magazine\frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Next.js build completes without errors
    - User confirms no clipping at top, left, right, or bottom canvas edges
    - User confirms ground line visible at correct vertical position
    - User confirms stage rows evenly spaced
  </acceptance_criteria>
  <done>Human has visually verified that all canvas edges show full content with no clipping, and the geometry looks correct</done>
</task>

</tasks>

<verification>
- grep confirms PADDING_Y = 60, CANVAS_HEIGHT = 650, HORIZONTAL_PADDING = 40
- STAGE_HEIGHT formula produces 68.75 (unchanged from before)
- GROUND_Y formula produces 335 (shifted down 30px from 305)
- Visual inspection confirms no clipping at any canvas edge
</verification>

<success_criteria>
1. All nodes, labels, and ticks fully visible at every canvas edge — zero clipping (CANVAS-03)
2. Top axis labels (Orbit, Fly, etc.) fully visible with clearance above
3. Ground line and below-ground zone at correct vertical position
4. Stage spacing unchanged (68.75px per stage)
5. Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-canvas-geometry-foundation/07-01-SUMMARY.md`
</output>
