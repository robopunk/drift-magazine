---
phase: 06-automation-end-to-end-validation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: true
requirements: [SCHED-03, OPS-02]

must_haves:
  truths:
    - "Second agent run completes without errors (consistent, reproducible execution)"
    - "GitHub account notification settings are configured to email on Actions failure"
  artifacts:
    - path: "GitHub Actions run #2"
      provides: "Second successful agent execution proving consistency"
      contains: "Exit code 0 or equivalent green checkmark"
  key_links:
    - from: "gh workflow run agent-run.yml (second trigger)"
      to: "GitHub Actions runner"
      via: "workflow_dispatch trigger"
      pattern: "Drift Agent.*Bi-weekly Research Run"
    - from: "GitHub Actions failure event"
      to: "Repository owner email"
      via: "GitHub native notification settings"
      pattern: "Settings.*Notifications.*Actions"
---

<objective>
Trigger a second agent run to prove consistent execution (monetization gate condition #3), and document that GitHub's native email-on-failure notifications are enabled for OPS-02.

Purpose: Validates SCHED-03 (second clean run = agent stability proven) and OPS-02 (failure alerts via GitHub email).
Output: Two green workflow runs visible in GitHub Actions. OPS-02 documented as satisfied via account settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-end-to-end-validation/06-CONTEXT.md
@.planning/phases/06-automation-end-to-end-validation/06-01-SUMMARY.md
@.github/workflows/agent-run.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger second workflow_dispatch and monitor to completion</name>
  <files></files>
  <read_first>
    - .planning/phases/06-automation-end-to-end-validation/06-01-SUMMARY.md (get run 1 results and any issues encountered)
    - .github/workflows/agent-run.yml (confirm workflow unchanged)
  </read_first>
  <action>
    1. Trigger the second workflow run via gh CLI:
       ```
       gh workflow run agent-run.yml --repo robopunk/drift-magazine
       ```

    2. Wait 10 seconds, then get the new run ID (must be different from run 1):
       ```
       gh run list --repo robopunk/drift-magazine --workflow=agent-run.yml --limit=2 --json databaseId,status,conclusion,createdAt
       ```
       The most recent run (highest databaseId or latest createdAt) is run 2.

    3. Watch the run until completion (same approach as Plan 01):
       ```
       gh run watch <RUN_ID_2> --repo robopunk/drift-magazine --exit-status
       ```
       Or poll with `gh run view` every 30 seconds, timeout 15 minutes.

    4. Verify conclusion is "success":
       ```
       gh run view <RUN_ID_2> --repo robopunk/drift-magazine --json status,conclusion
       ```

    5. Capture log output for evidence:
       ```
       gh run view <RUN_ID_2> --repo robopunk/drift-magazine --log
       ```

    6. Confirm both runs are now visible:
       ```
       gh run list --repo robopunk/drift-magazine --workflow=agent-run.yml --limit=5 --json databaseId,status,conclusion,createdAt
       ```
       At least 2 runs should show conclusion=success.

    IMPORTANT: Same rate limit considerations as Plan 01. If the run fails due to Anthropic
    rate limits (30k TPM), wait 5 minutes and retry once.
  </action>
  <verify>
    <automated>gh run list --repo robopunk/drift-magazine --workflow=agent-run.yml --limit=2 --json conclusion --jq '.[].conclusion' | grep -c "success"</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=agent-run.yml --limit=2 --json conclusion --jq '.[].conclusion'` outputs "success" twice (2 lines)
    - Run 2 has a different databaseId than Run 1
    - Run 2 log does NOT contain "AuthenticationError" or "401" or "403"
    - Run 2 log contains "Signal" or "signal" (evidence of signal processing)
  </acceptance_criteria>
  <done>Second GitHub Actions run completes with conclusion=success, proving consistent reproducible execution (SCHED-03 satisfied, monetization gate condition #3 cleared)</done>
</task>

<task type="auto">
  <name>Task 2: Document OPS-02 (GitHub email-on-failure notifications)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-automation-end-to-end-validation/06-CONTEXT.md (D-01 and D-02: GitHub native email, no workflow changes)
  </read_first>
  <action>
    Per D-01 and D-02 from CONTEXT.md: OPS-02 is satisfied by GitHub's native email-on-failure
    notification system. No workflow changes or custom notification steps are needed.

    1. Verify the workflow does NOT need modification — it already triggers on failure natively:
       GitHub sends email notifications to the repository owner when a workflow run fails,
       as long as the account has Actions notifications enabled in Settings > Notifications.

    2. Check if the robopunk account has watching/notifications on the repo:
       ```
       gh api repos/robopunk/drift-magazine --jq '.permissions'
       ```
       (Owner permissions confirm the account receives failure notifications by default)

    3. Document in the SUMMARY that OPS-02 is satisfied by:
       - GitHub account: robopunk (repository owner)
       - Notification path: GitHub Settings > Notifications > Actions > "Send notifications for failed workflows only" (or "All notifications")
       - No agent-run.yml modification required (per D-01)
       - Evidence: robopunk is the repo owner, GitHub sends failure emails to owners by default

    4. Optionally, verify by checking the notification subscription:
       ```
       gh api repos/robopunk/drift-magazine/subscription --jq '{subscribed, reason}'
       ```

    This task produces documentation evidence only — no file modifications.
  </action>
  <verify>
    <automated>gh api repos/robopunk/drift-magazine --jq '.owner.login' | grep -q "robopunk"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/robopunk/drift-magazine --jq '.owner.login'` outputs "robopunk"
    - No modifications were made to .github/workflows/agent-run.yml
    - SUMMARY documents that OPS-02 is satisfied via GitHub native notifications (per D-01, D-02)
  </acceptance_criteria>
  <done>OPS-02 documented as satisfied: robopunk is repo owner, GitHub natively emails on workflow failure, no workflow changes needed (per D-01, D-02)</done>
</task>

</tasks>

<verification>
- Two successful workflow runs visible in `gh run list` for agent-run.yml
- OPS-02 documented via GitHub native notifications (no code changes)
- Monetization gate condition #3 cleared (2 clean runs)
</verification>

<success_criteria>
1. Second workflow run completes with conclusion=success (SCHED-03)
2. Two distinct successful runs visible in Actions history
3. OPS-02 documented as satisfied via GitHub native email notifications (per D-01, D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-end-to-end-validation/06-02-SUMMARY.md`
</output>
