---
phase: 06-automation-end-to-end-validation
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03]

must_haves:
  truths:
    - "Draft signals created by agent runs are approved and visible on the live Vercel frontend"
    - "Confidence badges display correctly on approved signals (color-coded by confidence level)"
    - "Sandoz company page at /company/sdz shows 6 objectives and 51+ total signals"
  artifacts:
    - path: "https://drift-magazine.vercel.app/company/sdz"
      provides: "Live Sandoz company page with all signals"
      contains: "6 objectives, 51+ signals including approved agent drafts"
  key_links:
    - from: "agent.py --approve <id>"
      to: "Supabase signals table (is_draft=false)"
      via: "UPDATE signals SET is_draft=false"
      pattern: "is_draft.*false"
    - from: "Supabase signals (is_draft=false)"
      to: "https://drift-magazine.vercel.app/company/sdz"
      via: "Next.js server-side fetch from Supabase"
      pattern: "signals.*classification.*confidence"
---

<objective>
Approve draft signals from both agent runs, then verify the full end-to-end flow: approved signals appear on the live Vercel frontend with correct confidence badges, and the Sandoz page shows 6 objectives with 51+ total signals.

Purpose: Validates E2E-01 (signal flows agent to frontend), E2E-02 (confidence badges correct), and E2E-03 (Sandoz page complete with 51+ signals).
Output: All agent-produced draft signals approved, live site verified showing complete Sandoz data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-end-to-end-validation/06-CONTEXT.md
@.planning/phases/06-automation-end-to-end-validation/06-01-SUMMARY.md
@.planning/phases/06-automation-end-to-end-validation/06-02-SUMMARY.md
@.planning/phases/05-supabase-verification-deployment/VERIFICATION.md
</context>

<interfaces>
<!-- agent.py CLI for signal approval -->
From backend/agent.py:
  --review              Show all draft signals pending review (is_draft=true)
  --approve <signal-id> Approve a signal (set is_draft=false, mark reviewed)
  --reject <signal-id>  Delete a draft signal

<!-- Sandoz seed data baseline from CLAUDE.md -->
6 active objectives with 51 total signals (11+9+7+11+5+8) already in DB as is_draft=false
9 draft signals from Phase 5 + ~9 per agent run = ~27 additional drafts to approve

<!-- Frontend routing -->
Sandoz company page: https://drift-magazine.vercel.app/company/sdz
Ticker-based routing: /company/${ticker.toLowerCase()} where ticker=SDZ
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: List and approve all draft signals from agent runs</name>
  <files></files>
  <read_first>
    - .planning/phases/06-automation-end-to-end-validation/06-01-SUMMARY.md (signal IDs from run 1)
    - .planning/phases/06-automation-end-to-end-validation/06-02-SUMMARY.md (signal IDs from run 2)
    - .planning/phases/05-supabase-verification-deployment/VERIFICATION.md (9 original draft signal IDs)
  </read_first>
  <action>
    1. List all current draft signals:
       ```
       cd backend && py agent.py --review
       ```
       Record the full list of signal IDs. Expected: ~27 drafts (9 from Phase 5 + ~9 from run 1 + ~9 from run 2).

    2. Approve EVERY draft signal by running --approve for each ID:
       ```
       cd backend && py agent.py --approve <signal-id-1>
       cd backend && py agent.py --approve <signal-id-2>
       ... (repeat for all draft signal IDs)
       ```
       Each --approve call sets is_draft=false and marks reviewed_by/reviewed_at in Supabase.

       If there are many signals, batch the commands:
       ```
       cd backend
       for id in <id1> <id2> <id3> ...; do py agent.py --approve $id; done
       ```

    3. After all approvals, verify no drafts remain:
       ```
       cd backend && py agent.py --review
       ```
       Expected output: "No signals pending review" or equivalent empty state.

    4. Document total signals approved (count) and their classifications/confidence scores.

    NOTE: The Sandoz seed data already has 51 signals with is_draft=false in the DB.
    Approving the ~27 agent-produced drafts brings the total to ~78 signals (51 seed + 27 approved).
    This exceeds the E2E-03 requirement of 51+ signals.
  </action>
  <verify>
    <automated>cd backend && py agent.py --review 2>&1 | grep -i "no signals\|0 draft\|pending review" || echo "DRAFTS STILL EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - `py agent.py --review` output indicates zero remaining draft signals (contains "No signals" or "0 draft")
    - At least 18 signals were approved (9 from Phase 5 + at least 9 from the two runs)
    - Each --approve command exited without error
  </acceptance_criteria>
  <done>All draft signals from Phase 5 and both agent runs are approved (is_draft=false), zero drafts remaining</done>
</task>

<task type="auto">
  <name>Task 2: Verify E2E signal flow and confidence badges on live site</name>
  <files></files>
  <read_first>
    - .planning/phases/05-supabase-verification-deployment/VERIFICATION.md (baseline: what the site showed before approvals)
  </read_first>
  <action>
    1. Verify the live site reflects the newly approved signals.
       Fetch the Sandoz company page and check for signal data:
       ```
       curl -s https://drift-magazine.vercel.app/company/sdz | grep -o "reinforced\|reframed\|retired_silent\|softened\|absent\|stated\|achieved" | sort | uniq -c
       ```
       This shows the signal classifications present in the HTML. After approval, the page should
       include the agent-produced classifications (reinforced, reframed, retired_silent at minimum).

    2. Verify confidence badges are present:
       ```
       curl -s https://drift-magazine.vercel.app/company/sdz | grep -oi "confidence\|/10\|badge"
       ```
       The page should contain confidence score indicators. The exact rendering depends on the
       frontend components, but confidence values (7/10, 8/10, 10/10) should be present in the HTML
       or in the RSC payload.

    3. Verify 6 objectives are displayed:
       ```
       curl -s https://drift-magazine.vercel.app/company/sdz | grep -oi "objective\|biosimilar\|pipeline\|manufacturing\|margin\|emerging" | sort | uniq -c
       ```
       Expected: references to all 6 Sandoz objectives (Global Biosimilar Leadership, US Biosimilar
       Penetration, Emerging Markets, Next-Wave Pipeline, Manufacturing Simplification, Margin Expansion).

    4. Check that the total signal count exceeds 51. If the page renders a signal count anywhere:
       ```
       curl -s https://drift-magazine.vercel.app/company/sdz | grep -oP "\d+ signals?"
       ```

    5. Document E2E-01 evidence: signal created by agent in GitHub Actions -> stored in Supabase -> approved -> visible on live frontend.

    6. Document E2E-02 evidence: confidence badges render with correct values (7/10, 8/10, 10/10 matching the agent output from run logs).

    NOTE: The Next.js frontend may use server-side rendering with RSC (React Server Components).
    Signal data might be in the RSC payload rather than plain HTML. If grep doesn't find classifications
    in the HTML, try checking the RSC stream:
    ```
    curl -s https://drift-magazine.vercel.app/company/sdz -H "RSC: 1" | grep -o "reinforced\|confidence"
    ```
  </action>
  <verify>
    <automated>curl -s https://drift-magazine.vercel.app/company/sdz | grep -qi "sandoz" && curl -s https://drift-magazine.vercel.app/company/sdz | grep -qi "reinforced\|reframed\|retired" && echo "E2E PASS" || echo "E2E FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `curl https://drift-magazine.vercel.app/company/sdz` returns HTTP 200
    - Page HTML or RSC payload contains at least one of: "reinforced", "reframed", "retired_silent" (agent-produced classifications)
    - Page contains references to at least 5 of the 6 objectives (Biosimilar, Pipeline, Manufacturing, Margin, Emerging)
    - Page contains "Sandoz" in the response body
  </acceptance_criteria>
  <done>Agent-produced signals visible on live frontend (E2E-01), confidence data present (E2E-02), Sandoz page shows objectives and signals (partial E2E-03 automated check)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Sandoz page — 6 objectives, 51+ signals, correct badges</name>
  <files></files>
  <action>
    Present the user with the verification checklist below. This is a human-only visual check.
    The executor pauses here and waits for user confirmation before marking E2E-03 as satisfied.
  </action>
  <what-built>
    Full end-to-end signal pipeline: GitHub Actions ran agent.py twice, agent produced draft signals
    in Supabase, all drafts were approved via CLI, and the live Vercel frontend at
    https://drift-magazine.vercel.app/company/sdz now displays the complete Sandoz data including
    agent-produced signals with confidence badges.
  </what-built>
  <how-to-verify>
    1. Open https://drift-magazine.vercel.app/company/sdz in your browser
    2. Verify 6 objectives are visible:
       - Global Biosimilar Leadership
       - US Biosimilar Penetration
       - Emerging Markets Volume Growth
       - Next-Wave Biosimilar Pipeline
       - Manufacturing Network Simplification
       - Margin Expansion to 24-26%
    3. Count total signals across all objectives — should be 51 or more
       (51 seed signals + ~27 approved agent signals = ~78 total)
    4. Check confidence badges on the newly approved signals:
       - Reinforced signals should show 10/10 confidence
       - Reframed signals should show 7/10 confidence
       - Retired_silent signals should show 8/10 confidence
    5. Verify the landing page at https://drift-magazine.vercel.app also shows Sandoz data
    6. Confirm no visual errors, broken layouts, or missing data
  </how-to-verify>
  <verify>User types "approved" or describes issues</verify>
  <done>Human confirms Sandoz page shows 6 objectives, 51+ signals, and correct confidence badges (E2E-03 fully satisfied)</done>
  <resume-signal>Type "approved" if the Sandoz page shows 6 objectives, 51+ signals, and correct confidence badges. Otherwise describe what is missing or incorrect.</resume-signal>
</task>

</tasks>

<verification>
- All draft signals approved (zero remaining in --review)
- Live site at /company/sdz contains agent-produced signal classifications
- Human confirms 6 objectives, 51+ signals, correct confidence badges
- Full pipeline verified: GitHub Actions -> agent.py -> Supabase -> Vercel frontend
</verification>

<success_criteria>
1. Agent-produced signals flow from Supabase to live frontend display (E2E-01)
2. Confidence badges display correctly with accurate values (E2E-02)
3. Sandoz company page shows 6 objectives and 51+ total signals (E2E-03) — confirmed by human
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-end-to-end-validation/06-03-SUMMARY.md`
</output>
